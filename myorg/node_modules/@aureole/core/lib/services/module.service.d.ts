import { Repository } from 'typeorm';
import { SysModule } from '../entities/sys_module.entity';
export declare class ModuleService {
    private readonly moduleRepository;
    constructor(moduleRepository: Repository<SysModule>);
    getModules(context: any): Promise<SysModule[]>;
    getModule(context: any): Promise<SysModule[]>;
}

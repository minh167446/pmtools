import { Repository } from 'typeorm';
import { SysMaster } from '../entities/sys_master.entity';
export declare class MasterService {
    private readonly masterRepository;
    constructor(masterRepository: Repository<SysMaster>);
    getMasters(context: any): Promise<SysMaster[]>;
    getMaster(context: any): Promise<SysMaster[]>;
}

import { Builder, BuildEvent, BuilderConfiguration, BuilderContext } from '@angular-devkit/architect';
import { WebpackDevServerBuilder } from '@angular-devkit/build-webpack';
import { Observable } from 'rxjs';
export interface WebDevServerOptions {
    host: string;
    port: number;
    publicHost?: string;
    ssl: boolean;
    sslKey?: string;
    sslCert?: string;
    proxyConfig?: string;
    buildTarget: string;
    open: boolean;
    liveReload: boolean;
    watch: boolean;
}
export default class WebDevServerBuilder implements Builder<WebDevServerOptions> {
    private context;
    webpackDevServerBuilder: WebpackDevServerBuilder;
    root: string;
    constructor(context: BuilderContext);
    run(builderConfig: BuilderConfiguration<WebDevServerOptions>): Observable<BuildEvent>;
    private getBuildOptions;
    private getBuildBuilderConfig;
}

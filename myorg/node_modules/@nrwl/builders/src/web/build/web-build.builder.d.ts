import { Builder, BuildEvent, BuilderConfiguration, BuilderContext } from '@angular-devkit/architect';
import { WebpackBuilder } from '@angular-devkit/build-webpack';
import { Observable } from 'rxjs';
import { BuildBuilderOptions } from '../../utils/types';
import { OutputHashing, StylePreprocessorOptions, Budget } from '@angular-devkit/build-angular';
export interface WebBuildBuilderOptions extends BuildBuilderOptions {
    index: string;
    budgets: Budget[];
    baseHref: string;
    deployUrl: string;
    polyfills?: string;
    es2015Polyfills?: string;
    scripts: string[];
    styles: string[];
    vendorChunk?: boolean;
    commonChunk?: boolean;
    outputHashing?: OutputHashing;
    stylePreprocessingOptions?: StylePreprocessorOptions;
}
export default class BuildWebBuilder implements Builder<WebBuildBuilderOptions> {
    private context;
    webpackBuilder: WebpackBuilder;
    root: string;
    constructor(context: BuilderContext);
    run(builderConfig: BuilderConfiguration<WebBuildBuilderOptions>): Observable<BuildEvent>;
}
